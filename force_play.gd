extends AnimationPlayer

#idk if this is necessary but sometimes godot doesn't listen so
func _ready():
	play("fire")
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
